/**
 * Authenticated admin waitlist-oversight route.
 */
import { AdminWaitlistPanel } from "@/components/modules/admin/admin-waitlist-panel";

/**
 * Render the read-only admin pre-launch waitlist directory.
 */
export default function AdminWaitlistPage() {
  return <AdminWaitlistPanel />;
}
