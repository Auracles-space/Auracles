/**
 * Authenticated admin Credential review route.
 */
import { AdminCredentialReviewPanel } from "@/components/modules/admin/admin-credential-review-panel";

/**
 * Render the admin credential review queue with filter and verify/reject controls.
 */
export default function AdminCredentialsPage() {
  return <AdminCredentialReviewPanel />;
}
