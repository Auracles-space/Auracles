"""Projects API router placeholder.

Phase 4a Slice 1 only introduces schema and module boundaries. Endpoint
handlers are added in later project slices once their service behavior exists.
"""

from fastapi import APIRouter

router = APIRouter(prefix="/projects", tags=["Projects"])
